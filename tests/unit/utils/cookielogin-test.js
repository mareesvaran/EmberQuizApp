import cookielogin from 'quiz/utils/cookielogin';
import { module, test } from 'qunit';

module('Unit | Utility | cookielogin');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cookielogin();
  assert.ok(result);
});
